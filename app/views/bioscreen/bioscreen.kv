#:kivy 2.1.0

#: import hex kivy.utils.get_color_from_hex


<BioScreen>:

    bio_list: bio_list


    BoxLayout:
        size: root.width, root.height
        orientation: "vertical"
        spacing: 50
        padding: 50

        canvas:
            Color:
                rgba: hex("#E8DDD4")
            Rectangle:
                size: self.size
                pos: self.pos

        Title:
            text: "[u]Biographies[/u]"
            font_size: "50sp"
            pos_hint: {"center_y": 0.5, "center_x": 0.5}
            size_hint: 0.4, 0.15
            markup: True

        # Grid Menu
        ScrollView:
            do_scroll_x: False
            do_scroll_y: True
            dscroll_timeout: 250

            GridLayout:
                id: bio_list
                size_hint_y: None
                cols: 3
                row_default_height: root.height * 0.3
                height: self.minimum_height
                spacing: [56, 24]
                padding: [48, 0]

        # Bottom buttons
        BoxLayout:
            orientation: "horizontal"
            size_hint: 1, 0.1
            padding: [144, 0]
            spacing: 560

            ArrowLeftButton:
                on_release: root.manager.current = "home_screen"

            ArrowRightButton:
                label_message: "Start game"
                on_release:
                    root.manager.current = "menu_screen"
                    root.manager.ids["menu_screen"].populate_screen()
